{
  "name": "tashizan-jump",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "子ども向け足し算ランナー。答えをタップして跳び、障害物を越える。",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "build:single": "npm run build && node tools/build-single.mjs",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "verify": "node tools/verify-questions.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
